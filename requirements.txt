# Dépendances Python du projet (à affiner au fil des étapes).

# data/ (étape 1 — collecte et préparation des données)
requests
numpy
opencv-python-headless
kaggle  # téléchargement de SARD (data/scripts/download_sard.py)

# tests
pytest

# ai_detection (étape 2 — entraînement du modèle de détection)
ultralytics  # installe torch automatiquement (CPU par défaut sur cet environnement)

# mapping / photogrammétrie (étape 3-4, pas encore utilisé)
# opendronemap (via CLI/Docker, pas un package pip)
# rasterio
# geopandas

# dashboard backend (étape 5, pas encore utilisé)
# fastapi
# uvicorn
# sqlalchemy
# geoalchemy2
