# tests — Tests transverses et d'intégration

Statut : à faire (principalement à partir de l'étape 6 de la feuille de route).

Ce dossier contiendra les tests d'intégration du pipeline (`pipeline/orchestrator.py`) sur
données simulées, ainsi que les tests unitaires transverses qui ne trouvent pas naturellement
leur place dans un module spécifique (chaque module pourra aussi avoir ses propres tests
locaux).
